@echo off
chcp 65001 > nul
cd /d "%~dp0"
rem ============================================================
rem  네이버 광고 — 3시간마다 가볍게 확인
rem
rem  입찰가·잔액 / 오늘 광고비 / 오늘 매출·이익 만 본다.
rem  대용량 보고서(매체·검색어·시간대)는 새벽 작업에서 받는다.
rem
rem  낮에 입찰가를 바꾸면 다음 날 새벽에야 잡히던 것을 고치려고
rem  만들었다 (2026-09-18 사용자: 수집 주기를 3시간마다로).
rem  약 20초면 끝난다.
rem ============================================================
if not exist logs mkdir logs
python -X utf8 -u tools\ad_watch.py --days 3 >> logs\ad_watch.log 2>&1
